server:
    port: 8888

spring:
    application:
        name: config-server
    profiles:
        active: native
    cloud:
        config:
            server:
                native:
                    search-locations: file:///D:/Уник/microservices/Spring_Cloud_Config_Server_1/config-repo

management:
    endpoints:
        web:
            exposure:
                include: health,info
